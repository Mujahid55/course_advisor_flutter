name: course_advisor
description: "AI-powered academic assistant that helps students with their course syllabus."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  file_picker: ^8.1.7
  window_manager: ^0.4.3
  google_fonts: ^6.2.1
  uuid: ^4.5.1
  flutter_markdown: ^0.7.4
  shared_preferences: ^2.3.0
  fl_chart: ^0.68.0
  go_router: ^14.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
